version: 0.2

env:
  variables:
    FUNCTION_NAME: "create-client-lambda"
    AWS_REGION: "us-east-1"

phases:
  install:
    runtime-versions:
      nodejs: 20
    commands:
      - echo Installing NPM dependencies...
      - npm install
  build:
    commands:
      - echo Building...
      - npm run build